# Supabase Configuration (Optional)
# Leave empty or set to 'mock' to use localStorage instead
# For production with real database, set your Supabase credentials here
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Admin Password (for inquiry board access)
# Default: admin123 (change this for production!)
VITE_ADMIN_PASSWORD=

# DEPLOYMENT INSTRUCTIONS:
# 1. For local development: No need to set these (uses localStorage)
# 2. For production deployment:
#    - Set VITE_ADMIN_PASSWORD to a secure password
#    - Optionally set Supabase credentials if using real database
#    - Configure these in your hosting platform's environment variables:
#      * Netlify: Site settings → Environment variables
#      * Vercel: Project Settings → Environment Variables
#      * GitHub Pages: Not supported (use localStorage mode)
